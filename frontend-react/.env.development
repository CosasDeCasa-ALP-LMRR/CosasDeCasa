# RF12 / RNF7 — URL base de la API (desarrollo)
# El proxy de Vite redirige /auth/*, /identity/*, /match/*, etc. a https://localhost:3000
# por lo que la baseURL debe estar vacía en desarrollo (Vite actúa como proxy).
VITE_API_URL=